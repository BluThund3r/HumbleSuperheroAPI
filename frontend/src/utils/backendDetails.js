/* This file contains useful details about the backend (e.g. the base URL for all API calls) */

const backendHost = "localhost";
const backendPort = 8080;
export const baseURL = `http://${backendHost}:${backendPort}`;
